{
  "name": "pacman-ghost-ai",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/parity.test.js",
    "test:e2e": "playwright test",
    "test:all": "npm test && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
